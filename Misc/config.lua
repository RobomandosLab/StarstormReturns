-- -- survivors
-- settings.enable_chars = true
-- local enable_chars_checkbox = options:add_checkbox("enableChars")

-- enable_chars_checkbox:add_getter(function()
    -- return settings.enable_chars
-- end)

-- enable_chars_checkbox:add_setter(function(value)
    -- settings.enable_chars = value
    -- saved_settings:write(settings)
-- end)

-- -- items
-- settings.enable_items = true
-- local enable_items_checkbox = options:add_checkbox("enableItems")

-- enable_items_checkbox:add_getter(function()
    -- return settings.enable_items
-- end)

-- enable_items_checkbox:add_setter(function(value)
    -- settings.enable_items = value
    -- saved_settings:write(settings)
-- end)

-- -- enemies
-- settings.enable_enemies = true
-- local enable_enemies_checkbox = options:add_checkbox("enableEnemies")

-- enable_enemies_checkbox:add_getter(function()
    -- return settings.enable_enemies
-- end)

-- enable_enemies_checkbox:add_setter(function(value)
    -- settings.enable_enemies = value
    -- saved_settings:write(settings)
-- end)

-- -- elites
-- settings.enable_elites = true
-- local enable_elites_checkbox = options:add_checkbox("enableElites")

-- enable_elites_checkbox:add_getter(function()
    -- return settings.enable_elites
-- end)

-- enable_elites_checkbox:add_setter(function(value)
    -- settings.enable_elites = value
    -- saved_settings:write(settings)
-- end)

-- -- typhoon
-- settings.enable_typhoon = true
-- local enable_typhoon_checkbox = options:add_checkbox("enableTyphoon")

-- enable_typhoon_checkbox:add_getter(function()
    -- return settings.enable_typhoon
-- end)

-- enable_typhoon_checkbox:add_setter(function(value)
    -- settings.enable_typhoon = value
    -- saved_settings:write(settings)
-- end)

-- -- interactables
-- settings.enable_interactables = true
-- local enable_interactables_checkbox = options:add_checkbox("enableInteractables")

-- enable_interactables_checkbox:add_getter(function()
    -- return settings.enable_interactables
-- end)

-- enable_interactables_checkbox:add_setter(function(value)
    -- settings.enable_interactables = value
    -- saved_settings:write(settings)
-- end)

-- -- artifacts
-- settings.enable_artifacts = true
-- local enable_artifacts_checkbox = options:add_checkbox("enableArtifacts")

-- enable_artifacts_checkbox:add_getter(function()
    -- return settings.enable_artifacts
-- end)

-- enable_artifacts_checkbox:add_setter(function(value)
    -- settings.enable_artifacts = value
    -- saved_settings:write(settings)
-- end)

-- -- stages
-- settings.enable_stages = true
-- local enable_stages_checkbox = options:add_checkbox("enableStages")

-- enable_stages_checkbox:add_getter(function()
    -- return settings.enable_stages
-- end)

-- enable_stages_checkbox:add_setter(function(value)
    -- settings.enable_stages = value
    -- saved_settings:write(settings)
-- end)

-- -- beta content
-- settings.enable_beta = false
-- local enable_beta_checkbox = options:add_checkbox("enableBeta")

-- enable_beta_checkbox:add_getter(function()
    -- return settings.beta_stages
-- end)

-- enable_beta_checkbox:add_setter(function(value)
    -- settings.enable_beta = value
    -- saved_settings:write(settings)
-- end)